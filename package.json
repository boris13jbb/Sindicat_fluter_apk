{
  "name": "sindicat-flutter-firebase-rules",
  "private": true,
  "type": "module",
  "scripts": {
    "test:rules": "node --test --test-concurrency=1 firebase_rules_test/*.test.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0"
  }
}
